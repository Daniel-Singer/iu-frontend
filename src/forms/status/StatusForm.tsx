import { Select, Stack } from '@mantine/core';

const StatusForm = () => {
  return (
    <form>
      <Stack>
        <Select label="Status" />
      </Stack>
    </form>
  );
};

export default StatusForm;
